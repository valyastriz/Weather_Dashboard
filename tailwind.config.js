/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.html",    // Include all HTML files in src directory and its subdirectories
    "./src/**/*.js",      // Include all JS files in src directory and its subdirectories
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}